server:
  host: 0.0.0.0
  port: 8080

pages:
  - name: Home
    columns:
      - size: small
        widgets:
          - type: weather
            location: Bengaluru, India
          - type: calendar

      - size: medium
        widgets:
          - type: rss
            feeds:
              - url: https://rss.nytimes.com/services/xml/rss/nyt/World.xml
              - url: https://feeds.bbci.co.uk/news/rss.xml
            limit: 10

  - name: Homelab
    columns:
      - widgets:
          - type: bookmarks
            items:
              - title: Immich
                url: http://192.168.122.130:2283/

          - type: docker   # if you run Docker containers
            # or use the "glances" widget for system monitoring
